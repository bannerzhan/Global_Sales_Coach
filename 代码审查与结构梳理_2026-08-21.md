# Global Sales Coach — 全代码审查与结构梳理报告

> 审查日期：2026-08-21 ｜ 代码基线：main @ 63106bd（70 个 TS/TSX 源文件）
> 审查方法：双轴 review（Standards 代码质量轴 / Spec 需求覆盖轴，并行子代理 + 主控复核关键指控）

---

## 一、项目概况

外贸业务员的 AI 销售训练应用。核心闭环：**画像/目标 → 场景生成 → 角色扮演对练（AI 扮客户）→ 逐句复盘 → FSRS 技能调度**。附加：基线评估、技能图谱、模拟电话（新建模块）、翻译辅助、中英双语。

| 维度 | 现状 |
|---|---|
| 技术栈 | Next.js 16.3.1 App Router + Turbopack、React 19、Tailwind v4、PostgreSQL 16 + pgvector、Auth.js v5 beta（Credentials 多用户） |
| LLM | 火山方舟 Ark（OpenAI 兼容 SDK v7），全站统一 **flash** 档（deepseek-v4-flash） |
| 语音 | 火山 openspeech REST（ASR/TTS）+ **浏览器端 Whisper / speechSynthesis（与 spec 冲突，见 §5）** |
| 部署 | Docker Compose（routec 变体：app + db + caddy 自签 3443），tarball → scp → rebuild 流程 |
| 测试 | scripts/smoke*.ts + test-fsrs.ts + test-no-hallucination.ts（tsx 手动跑，无 CI） |
| 版本控制 | GitHub 公开仓库 main 分支（本地 .git 曾损坏恢复过） |

**核心抽象（值得保留的设计）：**
1. **输出契约链** `src/lib/llm/contract.ts` — 业务侧只给 Zod schema + 业务校验 + fallback，记账/重试/降级/成本护栏全自动。这是全项目最稳的层。
2. **双后端 repo**（`isDbAvailable()` → PG / 本地 JSON 兜底）— 本机无 Docker 也能开发。
3. **四级成本护栏**（request/session/user_daily/global，fail-open 不阻断业务）。
4. **多用户隔离** — 所有 repo 读函数显式传 userId（DB 模式不传会 fallback 到 admin）。

---

## 二、目录结构总览

```
global-sales-coach/
├─ src/
│  ├─ app/                          # 页面 + API 路由（App Router）
│  │  ├─ page.tsx                   # 首页：画像摘要/基线/目标/入口卡/复习区
│  │  ├─ login/ register/           # 登录（Auth.js v5）/ 多用户注册（邀请码）
│  │  ├─ onboarding/                # 引导：画像表单 + LLM 目标建议 + 基线评估
│  │  ├─ practice/                  # 情景演练：列表 → 会话页(chat) → 复盘(review)
│  │  ├─ calls/                     # 模拟电话（新）：列表 → 新建简报 → 通话间 → 四维复盘
│  │  ├─ assessment/                # 基线评估（自评 + LLM 总评）
│  │  ├─ skills/                    # 技能图谱：3 维度 + 13 技能掌握度
│  │  ├─ api/
│  │  │  ├─ auth/[...nextauth]/     # Auth.js 端点
│  │  │  ├─ health/                 # 健康检查（DB/LLM/预算）
│  │  │  ├─ translate/              # 翻译接口（flash，记账）
│  │  │  └─ voice/ (asr|tts)        # 火山语音 REST 代理
│  │  └─ manifest.ts sw-register.ts # PWA
│  ├─ components/                   # voice-input/voice-play（语音）、translate-block/translate-box（翻译）、LanguageToggle
│  ├─ lib/
│  │  ├─ env.ts                     # Zod 环境变量校验（build 期固化，见硬坑#15）
│  │  ├─ db.ts                      # pg 连接池 + 事务
│  │  ├─ fsrs.ts                    # FSRS-4.5 纯函数引擎
│  │  ├─ llm/                       # provider(档位路由) / contract(契约链) / accounting(记账+护栏)
│  │  │  └─ 业务模块：scenario-gen / roleplay-reply / review / assessment / goal-suggest /
│  │  │     translate / call-script / call-reply / call-review / voice / lang
│  │  └─ repo/                      # storage(双后端选择器) + attempt/call/scenario/profile/goal/
│  │     skill-state/skills/assessment/types（DB 列 snake_case ↔ TS camelCase 映射）
│  ├─ auth.ts                       # Auth.js 实例（单文件，proxy 复用）
│  └─ proxy.ts                      # Next 16 路由守卫（未登录 → /login）
├─ db/init/                         # 01-extensions(pgvector) → 02-schema → 03-seed → 04-migration → 05-calls
├─ scripts/                         # smoke 系列冒烟 + fsrs/防幻觉单测
├─ Dockerfile / docker-compose*.yml / Caddyfile* / deploy.sh   # 部署体系
└─ 顶层 spec 文档                    # V0.1/V0.2/V0.3 Agent 开工指令 + PRD docx
```

---

## 三、已实现功能清单

| 功能 | 位置 | 状态 |
|---|---|---|
| Auth 登录 + 多用户注册（邀请码） | auth.ts / login / register | ✅ |
| Onboarding（画像 + LLM 目标建议 + 基线评估） | onboarding/* | ✅ |
| 演练闭环（场景生成→对练→复盘） | practice/* + llm | ✅ |
| 逐句解剖复盘（customerSentenceAnalysis） | review.ts | ✅ |
| FSRS-4.5 技能调度 + 技能图谱页 | fsrs.ts / skill-state / skills | ✅ |
| 基线评估（自评 + 总评） | assessment/* | ✅ |
| 成本护栏 + ai_runs 记账（LLM 文本类全覆盖） | accounting.ts | ✅（有漏洞见 §5） |
| 防幻觉（行为准则 + 不落空兜底） | review/assessment prompt | ✅ |
| PWA + 语言切换（zh/en） | manifest / LanguageToggle | ✅ |
| AI 文字块翻译 + 输入框翻译小框 | translate.* | ✅ |
| 模拟电话（文字版：档案库/简报/对练/四维复盘） | calls/* | ✅（语音未接） |
| 语音（TTS/ASR） | voice.* + api/voice/* | ⚠️ 偏离 spec |

---

## 四、Standards 审查报告（代码质量轴）

### 硬问题（按严重度）

1. **【最严重】语音突破"仅火山一家" + 禁端侧推理两条硬约束**
   `voice-input-button.tsx:20-23` 用 HuggingFace Whisper（transformers.js **浏览器端推理**，hf-mirror 加载 40MB 模型）做首选 ASR；`voice-play-button.tsx:16-33` 用浏览器原生 `speechSynthesis` 做首选 TTS，火山只做兜底。违反 V0.2 §8「不引入第三方 ASR·TTS」、V0.3 §3.5「不引入端侧推理模型」。已亲自核实。
   → 决定权在你：**接受现状（更新 spec）** 或 **回归火山一家**。端侧方案的优点是免费、国内可用、隐私好；缺点是 40MB 模型加载、识别质量不稳、违反既定架构。

2. **【严重】演练/引导主流程 LLM 调用不传 userId → user_daily 成本护栏名存实亡**
   `practice/actions.ts`（generateScenario、customerReply、reviewSession 均未传 userId）与 `onboarding/actions.ts`（suggestGoals、runAssessment 未传）。后果：`ai_runs.user_id=NULL`，`computeScopeSpend` 对 null 直接返回 0，**每日 5 元预算永不触发**。对比 `calls/actions.ts` 全部传了——新代码对，老代码漏。
   → 修法：6 处调用补 `userId: uid` 一行的事。

3. **【中】语音零记账**：`voice.ts` 的 ASR/TTS 不 `recordRun`、不过护栏；`ai_runs` 无 `stt_seconds/tts_chars` 字段；`audio_assets` 表未建。

4. **【中】自由对话只记账不过护栏**：`roleplay-reply.ts` / `call-reply.ts` 只 `recordRun` 不 `checkAllBudgets`（flash 便宜但语义上该一致）。

5. **【低】本地兜底 userId 写死**：`repo/assessment.ts:58-66` 本地分支写死 `LOCAL_USER_ID("1")`，多用户本地开发会串数据（生产走 DB 无碍）。

### Smell（判断项）

- **Duplicated Code**：`chat.tsx` 与 `call-view.tsx` 乐观更新逻辑几乎全同；review.ts / call-review.ts 对话格式化重复；`PURPOSE_LABEL` 在 call-script / call-review / 两个 calls 页面各写一遍。
- **Duplicated Code / 双源真相**：`skills.ts` 技能定义与 `03-seed.sql` 需手动同步，改一处漏一处。
- **Primitive Obsession**：accounting.ts 里 tier 类型放宽成 `"pro"|"turbo"|string`，`PRICING.default` 与 pro 定价重复。
- **Shotgun Surgery**：`actions.ts:226` 用动态 import 破循环依赖，说明模块边界有环（attempt 模块过胖）。
- **Message Chains / Middle Man**：部分页面组件层层透传 props。

---

## 五、Spec 覆盖审查报告（需求轴，对照 V0.1/V0.2/V0.3 开工指令）

### 缺失/未交付（按重要性）

| # | spec 要求 | 现状 | 来源 |
|---|---|---|---|
| 1 | **语音走服务端 WS 中继**（双向流式 + VoiceAdapter 接口） | 仅 REST 单次代理，无 WS、无流式 | V0.2 §3.1/§3.3 |
| 2 | **V0.3 全部语音进阶**：realtime 可打断通话、多音色路由、声音复刻（voice_profiles + scenarios.speaker_id）、barge-in、弱网韧性 | 全部缺失 | V0.3 |
| 3 | **记忆系统**：memories 表 + HNSW 已建但**零读写**，无 embedding 生成/提取/召回 | 缺失 | V0.1 §5 |
| 4 | **立刻重练（Immediate Retry）**：attempts.is_retry 字段在，无重练 action/按钮；retry_loop flag 空置 | 缺失 | V0.1 §1 DoD |
| 5 | **Learning Priority / LessonSpec / 每日课程调度**：lessons 表未用，createPractice 直接动态生成，无"今日课程" | 缺失 | V0.1 §3.1/§5 |
| 6 | **work_context seed**：粘贴真实产品/邮件/RFQ 的入口 | 恒为 null，无 UI | V0.1 §3.7 |
| 7 | **预置场景库（8-10 个）+ 审核入库流程** | 全动态生成 | V0.1 §7 |
| 8 | **Goal Interview 多轮访谈** | 单轮生成 3 目标 | V0.1 §5 |
| 9 | **语音成本字段 + 录音资产落库/回放** | ai_runs 无语音字段；audio_assets 未建 | V0.2 §4.1/4.2 |
| 10 | **voice_mode flag 真正生效**：前端无条件挂语音按钮，flag=false 也发起端侧语音 | 未检查 flag | V0.2 §4.4 |

### 实现与 spec 不符

- V0.2 §2 锁定 STT 为 `doubao-seed-asr-2.0`（WS 流式逐字）→ 实际用 `volc.bigasr.auc_turbo` 录音文件 REST 一次识别。
- V0.2 §8「不引入第三方 ASR·TTS（仅火山一家）」→ 前端 Whisper + speechSynthesis 优先（见 §4-1）。
- V0.1 §3.1「SRS 必须封装 SRSAdapter，业务不得直调 fsrs()」→ `skill-state.ts:78` 直调 `reviewFsrs`。
- V0.1 §5 REST 端点清单 → 全部改用 server actions，`/api/lesson/*`、`/api/memory/*`、`/api/progress` 未建。

### Scope creep（spec 未要求、你主动加的，建议保留）

模拟电话模块（customers/calls/call_reviews）｜ 多用户注册+邀请码｜ 翻译功能｜ 中英语言切换。

---

## 六、待实现功能清单（按优先级）

### P0 — 正确性（建议先修）

- [ ] **1. 成本护栏修复**：practice/onboarding 6 处 LLM 调用补 `userId`（一行/处）
- [ ] **2. 语音路线决策**：要么接受端侧方案并更新 spec，要么回火山一家
- [ ] **3. 语音记账**：voice.ts 补 recordRun + 成本字段

### P1 — spec 承诺未交付

- [ ] 4. 语音 WS 中继（流式 STT/TTS + VoiceAdapter 接口）
- [ ] 5. 模拟电话接 realtime 真语音（= V0.3 可打断通话的落点：/api/call/ws 中继 + 字幕 + 文字降级，即方案 Step 5-6）
- [ ] 6. 记忆系统：embedding 接入点 → extract_memory → 召回（首页可显示"我记得你上次…"）
- [ ] 7. 立刻重练：复盘页加"立刻重练"按钮（复用 is_retry 字段）
- [ ] 8. 每日课程调度：今日该练什么（Learning Priority + lessons 表）

### P2 — 产品增强

- [ ] 9. work_context 粘贴入口（真实产品/RFQ 喂给场景）
- [ ] 10. 预置场景库 + 审核入库
- [ ] 11. Goal Interview 多轮化
- [ ] 12. 翻译逐句对照（二期）
- [ ] 13. 客户档案 → 历史通话钻取
- [ ] 14. voice_mode flag 生效 + /api/health 语音连通性

### 部署/工程

- [ ] 15. Route A：域名 ridgehound.online + Caddy HTTPS（待 ICP 备案，deploy.sh 已就绪）
- [ ] 16. 仓库卫生：清理根目录残留（b2_out.txt / build_err.txt / .trash_logs / tsconfig.tsbuildinfo），补 .gitignore

---

## 七、总体结论

- **Standards 轴**：5 个硬问题（语音越界、userId 缺失、语音零记账、自由对话不过护栏、本地兜底写死）+ 若干 smell。最严重：**语音架构越界** 和 **user_daily 护栏失效**。
- **Spec 轴**：V0.1 文字闭环主体已落地；缺口集中在三块——**语音（V0.2/V0.3 近乎零落地）**、**记忆系统（表建了没代码）**、**调度类（Retry/Learning Priority）**；另有 4 项 scope creep 是你主动加的、建议保留。
- 一句话：**文字版核心闭环健康、扩展性不错；语音层偏离架构、成本护栏有洞、记忆与调度是下一阶段主战场。**

> 报告由双轴子代理审查 + 主控复核生成；标注"已亲自核实"的项均经过人工读码确认。
